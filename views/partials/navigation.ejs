<nav>
    <ul class="navigation">
        <li>
            <% if (title == 'Home') { %>
                <a id="active" href="/">Home</a>
            <% } else { %>
                <a href="/">Home</a>
            <% } %>
        </li>
        <li>
            <% if (title == 'Resume') { %>
                <a id="active" href="/resume">Resume</a>
            <% } else { %>
                <a href="/resume">Resume</a>
            <% } %>
        </li>
        <li>
            <% if (title == 'Projects') { %>
                <a id="active" href="/projects">Projects</a>
            <% } else { %>
                <a href="/projects">Projects</a>
            <% } %>
            <img class="dropbtn" src="/images/icons/angle-down-solid.svg" alt="Angle Down Icon">
            <div id="dropdown" class="dropdown-content project-names"></div>
        </li>
        <li class="login">
            <button onclick="document.getElementById('id01').style.display='block'">Login</button>
        </li>
    </ul>
</nav>